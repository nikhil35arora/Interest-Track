# 1. Hide the database secrets (CRITICAL)
.env
backend/.env

# 2. Ignore the massive library folders 
# (The server will reinstall these automatically from package.json)
node_modules/
backend/node_modules/
frontend/node_modules/

# 3. Ignore operating system junk
.DS_Store
Thumbs.db

# 4. Ignore local logs
npm-debug.log*